<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="paradigm_ehb.CommandCenter.WinUI.srvMgnt.Views.ProcessesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:paradigm_ehb.CommandCenter.WinUI.srvMgnt.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar with refresh -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="8" Grid.Row="0">
            <Button x:Name="RefreshButton"
                    Content="Refresh"
                    Click="RefreshButton_Click"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    Margin="0,0,4,0"/>
        </StackPanel>

        <ListView
            ItemsSource="{x:Bind processes, Mode=OneWay}"
            SelectionMode="Single"
            ShowsScrollingPlaceholders="True"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ProcessInfo">
                    <Grid>
                        <Grid.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Start Service" Click="ServiceStartMenuItem_Click" />
                                <MenuFlyoutItem Text="Stop Service" Click="ServiceStopMenuItem_Click" />
                                <MenuFlyoutItem Text="Restart Service" Click="ServiceRestartMenuItem_Click" />
                                <MenuFlyoutItem Text="Enable Service" Click="ServiceEnableMenuItem_Click" />
                                <MenuFlyoutItem Text="Disable Service" Click="ServiceDisableMenuItem_Click" />
                                <MenuFlyoutItem Text="View Properties" Click="ServiceViewMenuItem_Click" />
                            </MenuFlyout>
                        </Grid.ContextFlyout>

                                <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Ellipse
                            Grid.RowSpan="2"
                            Width="32"
                            Height="32"
                            Margin="6"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Fill="{ThemeResource ControlStrongFillColorDefaultBrush}"/>

                        <TextBlock Grid.Column="1"
                                   Text="{x:Bind ProcessId}"
                                   x:Phase="1"
                                   Style="{ThemeResource BaseTextBlockStyle}"
                                   Margin="12,6,0,0"/>

                        <TextBlock Grid.Column="1"
                                   Grid.Row="1"
                                   Text="{x:Bind ProcessName}"
                                   x:Phase="2"
                                   Style="{ThemeResource BodyTextBlockStyle}"
                                   Margin="12,0,0,6"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
