<UserControl
    x:Class="paradigm_ehb.CommandCenter.WinUI.Components.Reusable.Home_ServerOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:paradigm_ehb.CommandCenter.WinUI.Components.Reusable"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <MenuFlyout x:Key="colorMenuFlyout">
            <MenuFlyoutItem Text="Red" Tag="red"/>
            <MenuFlyoutItem Text="Green" Tag="green"/>
        </MenuFlyout>
    </UserControl.Resources>


    <Border Height="140" Width="200" CornerRadius="10" BorderBrush="White" BorderThickness="1"
            x:Name="rootBorder">
        <!-- <Border.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutItem Text="Open in CS" Icon="Go"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Shut Down" Icon="Stop"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Delete" Icon="Delete"/>
            </MenuFlyout>
        </Border.ContextFlyout> -->
        
        <Grid Tapped="Grid_Tapped">
            <Rectangle x:Name="serverBackground" Fill="White" Opacity="0"/>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="7">
                <Image Source="https://upload.wikimedia.org/wikipedia/commons/thumb/9/9e/UbuntuCoF.svg/1200px-UbuntuCoF.svg.png" Height="70" />
                <TextBlock x:Name="ServerNameText" HorizontalAlignment="Center" />
                <StackPanel Spacing="7" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Ellipse x:Name="StatusColor" Fill="Lime" Height="15" Width="15" />
                    <TextBlock x:Name="StatusText" Text="Online"></TextBlock>
                </StackPanel>
            </StackPanel>
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="PointerStates">
                
                <VisualState x:Name="Normal">
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="serverBackground" 
                            Storyboard.TargetProperty="Opacity" 
                            To="0" Duration="0:0:0.1" />
                    </Storyboard>
                </VisualState>
                
                <VisualState x:Name="PointerOver">
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="serverBackground" 
                            Storyboard.TargetProperty="Opacity" 
                            To="0.1" Duration="0:0:0.1" />
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Border>
</UserControl>
