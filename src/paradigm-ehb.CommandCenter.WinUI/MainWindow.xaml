<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="paradigm_ehb.CommandCenter.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:paradigm_ehb.CommandCenter.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Paradigm"
    x:Name="RootPage"
    SizeChanged="WindowSizeChanged"
    >

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="SimpleTitleBar"
                  Title="Paradigm"
                  Subtitle="v1.0"
                  IsBackButtonVisible="False"
                  Grid.Row="0" />

        <NavigationView x:Name="nvSample"
                Grid.Row="1"
                PaneDisplayMode="Left"
                IsPaneOpen="True"
                IsPaneToggleButtonVisible="False"
                ExpandedModeThresholdWidth="500"
                SelectionFollowsFocus="Enabled"
                IsTabStop="False"
                IsBackButtonVisible="Collapsed"
                IsSettingsVisible="False"
                ItemInvoked="NvSample_ItemInvoked">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home" Tag="HomePage" Icon="Home" />
                <NavigationViewItem Content="Settings" Tag="SettingsPage" Icon="Setting" />

                <NavigationViewItemHeader Content="Your Servers"/>

                <!-- Make parent nodes non-selectable (don't become Selected when clicked) -->
                <NavigationViewItem Content="EU Servers" Icon="Folder" SelectsOnInvoked="False">
                    <NavigationViewItem.MenuItems>
                        <NavigationViewItem Content="Server 1" Tag="EU1" Icon="Bullets" SelectsOnInvoked="True" />
                        <NavigationViewItem Content="Server 2" Tag="EU2" Icon="World" SelectsOnInvoked="True" />
                    </NavigationViewItem.MenuItems>
                </NavigationViewItem>

                <NavigationViewItem Content="US Servers" Icon="Folder" SelectsOnInvoked="False">
                    <NavigationViewItem.MenuItems>
                        <NavigationViewItem Content="Server A" Tag="US_A" Icon="World" SelectsOnInvoked="True" />
                        <NavigationViewItem Content="Server B" Tag="US_B" Icon="World" SelectsOnInvoked="True" />
                    </NavigationViewItem.MenuItems>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Content>
                <Frame x:Name="contentFrame" />
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Window>
